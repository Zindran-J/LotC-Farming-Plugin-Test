package commands;

import org.bukkit.command.Command;
import org.bukkit.command.CommandSender;
import org.jetbrains.annotations.NotNull;

public class croptrample {
    // This is currently not finished.

    //TODO:
    // Create a command that toggles crop trampling on/off for an individual player.
    // This will be /croptrample <enable/disable> <user>
    // The command must persist over restarts/shutdowns, and is NOT togglable by the player themselves.

    public boolean onCommand(@NotNull CommandSender commandSender, @NotNull Command command, @NotNull String s, @NotNull String[] strings) {
        return false;
    }
}
